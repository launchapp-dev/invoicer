{
  "name": "brain-db",
  "version": "1.0.0",
  "description": "SQLite structured data layer for brain knowledge base",
  "type": "module",
  "scripts": {
    "migrate": "node --experimental-strip-types migrate.ts",
    "query": "node --experimental-strip-types query.ts"
  },
  "dependencies": {
    "better-sqlite3": "^11.0.0",
    "gray-matter": "^4.0.3"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.0",
    "@types/node": "^22.0.0"
  }
}
